# angular-using-api
An app based on Angular 8.0.2 which lists users by calling the reqres.in API

# Resolving CORS
I resolved the CORS issue by installing a Mozilla extension called "CORS Everywhere". You can use similar extensions, to bypass the issue. An native Angular resolution might also be added, which I did not know at the time of writing the code. Feel free to add it by contributing.
